---
pool:
  name: Azure Pipelines
  vmImage: ubuntu-latest

trigger:
  - main

steps:
  - task: TerraformInstaller@1
    inputs:
      terraformVersion: 'latest'
  - bash: terraform init
    displayName: deploy vm
    workingDirectory: '*/4-subnet-1-vpc\ec2-instance' 
  

